import { PlaceholderScreen } from "../../../components/PlaceholderScreen";

export function CaptureScreen() {
  return (
    <PlaceholderScreen
      title="Capture"
      detail="Initial route for fast thought capture."
    />
  );
}
